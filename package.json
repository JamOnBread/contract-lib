{
  "name": "job",
  "version": "1.0.0",
  "type": "module",
  "description": "Can log \"hello world\" and \"goodbye world\" to the console!",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "dependencies": {
    "@types/chai": "^4.3.5",
    "@types/mocha": "^10.0.1",
    "chai": "^4.3.7",
    "lucid-cardano": "^0.10.6",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.1"
  },
  "devDependencies": {
    "@types/node": "^20.4.4",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.1.6"
  },
  "scripts": {
    "compile": "tsc -b ./tsconfig.cjs.json ./tsconfig.esm.json",
    "build:clean": "rm -rf ./dist",
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "ts-node example.ts --noemit",
    "ba": "npm-run-all build:clean compile && node ./scripts/prepare-package-json"
  }
}
